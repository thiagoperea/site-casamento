import 'package:flutter/material.dart';

class Ensaios extends StatelessWidget {
  const Ensaios({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Ensaios vão aqui!!"),
      ],
    );
  }
}
