import 'package:flutter/material.dart';

class ListaDePresentes extends StatelessWidget {
  const ListaDePresentes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Lista de Presentes!!"),
      ],
    );
  }
}
