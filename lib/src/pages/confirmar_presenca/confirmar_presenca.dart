import 'package:flutter/material.dart';

class ConfirmarPresenca extends StatelessWidget {
  const ConfirmarPresenca({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Confirmar Presença !")
      ],
    );
  }
}
